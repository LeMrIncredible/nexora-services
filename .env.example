# Nexora Environment Configuration Example
# Copy this file to `.env` and fill in your own values.  All automations and
# API handlers read configuration from environment variables.  Only `.env`
# should contain real secrets.  Do not commit `.env` to version control.

## Client Identification
# Name of the business (used in emails and logs)
CLIENT_NAME=Acme Plumbing
# Contact email for the business owner
CLIENT_EMAIL=owner@example.com
# Contact phone number for the business owner (E.164 format recommended)
CLIENT_PHONE=+15551234567

## Google Workspace Credentials
# Absolute path to a Google service account JSON credentials file.  This
# account must have permission to write to Google Sheets and send emails via
# Gmail API.  See GOOGLE_WORKSPACE_SETUP.md for setup details.
GOOGLE_SERVICE_ACCOUNT_KEYFILE=/path/to/service-account.json
# ID of the Google Sheet where audit results and leads will be stored.  The
# sheet should already exist and be shared with the service account.
LEADS_SHEET_ID=your-google-sheet-id

## Gmail SMTP (for notification emails)
# Email address used to send notifications (e.g. via Gmail SMTP).  Requires an
# app password if 2FA is enabled.  Leave blank to disable email
# notifications.
GMAIL_USER=
GMAIL_PASS=

## Automation Feature Toggles
# Set to `true` or `false` to enable or disable individual automations.
AUTOMATION_LEAD_CAPTURE_ENABLED=true
AUTOMATION_SMART_BOOKING_ENABLED=true
AUTOMATION_ESTIMATE_GENERATOR_ENABLED=true
AUTOMATION_REPUTATION_FOLLOWUP_ENABLED=true
AUTOMATION_INVOICE_TRACKING_ENABLED=true

## Server Port
# Change this to run the server on a different port.
PORT=3000
